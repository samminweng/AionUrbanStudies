class Utility {
    static cluster_topic_words_dict = {};
    // Build the map between clustered documents to topic words
    // Get document's cluster no
    static get_cluster_by_doc_id(total_clusters, doc_id){
        let c_w_dict = Utility.cluster_topic_words_dict[total_clusters];


    }

}
